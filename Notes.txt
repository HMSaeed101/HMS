
- Live updates card, NOW, What  im currently focused on, floating over side screen for readers to read
- Something map-like background for location things
- Reactive Typography
- Lines Squiggle - Blobs

==============================================

Navigation Stall:
"Toggle Menu" & "Back to Top"
The Mobile Menu Toggle did not reveal any navigation links in the tested viewport. The Back to Top button caused a timeout during testing, suggesting either a broken script or an excessively long animation that blocks the UI.

=====================================

Recommendations

1. Fix Routing: Ensure that relative paths (like projects/ and photography/) are correctly handled by your server and that the file names match (e.g., check if academics html is spelled correctly).

2. Implement Filtering: Connect your filter button event listeners to a function that toggles the display: none property (or equivalent) on the project/photo cards based on their category classes.

3. Optimize Scripts: Review the JavaScript for the "Back to Top" and "Mobile Menu" to ensure they aren't causing execution blocks.

4. Validate Assets: Check the Download CV path; while it didn't crash, ensure the ../Docs/ folder is correctly structured on your production server.
